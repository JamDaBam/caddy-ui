services:
  caddy-ui:
    build: .
    ports:
      - "3001:3001"
    environment:
      PORT: "3001"
      CADDYFILE_PATH: /etc/caddy/Caddyfile
      ENABLE_RELOAD: "false"
    volumes:
      - ./test/Caddyfile:/etc/caddy/Caddyfile
    restart: unless-stopped
